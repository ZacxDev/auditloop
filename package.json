{
  "name": "auditloop",
  "version": "0.1.0",
  "private": true,
  "description": "Build tooling for auditloop (Tailwind v4 CLI + vendored axe-core).",
  "scripts": {
    "css": "tailwindcss -i static/input.css -o static/output.css --minify"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@playwright/test": "^1.49.0",
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  },
  "dependencies": {
    "axe-core": "^4.12.1"
  }
}
